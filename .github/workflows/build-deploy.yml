name: 'Build & Deploy'
on: 
  release: 
    types: [published]
  workflow_dispatch: 
jobs:
  build: 
    name: 'Build & Publish'
    runs-on: ubuntu-latest
    steps: 
      - name: 'Checkout repository'
        uses: actions/checkout@v3
      
      - name: 'Set up Docker Buildx'
        uses: docker/setup-buildx-action@v2
      
      - name: 'Login to GitHub Registry'
        uses: docker/login-action@v2
        with:
          registry: ${{ secrets.DOCKER_REGISTRY }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_TOKEN }}
      
      - name: 'Build & Deploy Authentication Microservice'
        uses: docker/build-push-action@v4
        with:
          context: ./Backend
          file: ./Backend/Microservices/Authentication/src/Presentation/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_REGISTRY }}/${{ secrets.REGISTRY_USERNAME }}/authentication-microservice-api
          
      - name: 'Build & Deploy Files Microservice'
        uses: docker/build-push-action@v4
        with:
          context: ./Backend
          file: ./Backend/Microservices/Files/src/Presentation/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_REGISTRY }}/${{ secrets.REGISTRY_USERNAME }}/Dockerfiles-microservice-api

      - name: 'Build & Deploy Storage Microservice'
        uses: docker/build-push-action@v4
        with:
          context: ./Backend
          file: ./Backend/Microservices/Storage/src/Presentation/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_REGISTRY }}/${{ secrets.REGISTRY_USERNAME }}/storage-microservice-api
          
      - name: 'Build & Deploy API Gateway'
        uses: docker/build-push-action@v4
        with:
          context: ./Backend
          file: ./Backend/ApiGateways/OcelotApiGateway/Dockerfile
          push: true
          tags: ${{ secrets.DOCKER_REGISTRY }}/${{ secrets.REGISTRY_USERNAME }}/ocelot-api-gateway
